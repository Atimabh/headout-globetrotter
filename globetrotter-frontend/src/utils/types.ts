export type QuestionType = {
  city: string
  country: string
  clue: string[]
  fun_fact: string[]
  options: string[]
}

export type LeaderboardPlayerType = {
  username: string
  high_score: number
}
